alias eemerge="sudo elogind-inhibit emerge"

alias src="omz reload"

alias shutdown="sudo shutdown"
alias reboot="sudo reboot"

alias ls="exa"
alias la="exa -la"
alias tree="exa --tree"
alias cat="bat"

alias diff="diff --color=auto"
alias grep="grep --color=auto"
alias ip="ip -color=auto"
